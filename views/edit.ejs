<html>
  <head>
    <% include ./partials/head %>
  </head>
  <body>
    <header>
      <% include ./partials/header %>
    </header>
    <div class="container">
      <% var url = "/cities/" + city.id + "?_method=PUT" %>
      <form method="POST" action=<%= url %> >
        <input required name="name" value= <%= city.name %> />
        <a href="/cities" class="btn red">Cancel</a>
        <button class="btn">Save</button>
      </form>
    </div>
  </body>
</html>